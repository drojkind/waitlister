%br
- if @auth.present?
  %section.reservation
    =link_to("Make a Reservation", new_reservation_path, remote: true, class: "button radius")
#form
#waitlist


.small-3.columns
  %ol
    - @reservations.each do |reservation|
      %li
        %form{:action => "/reservations/sendtext", :method => "post"}
          = reservation.name
          = reservation.phone
          = reservation.est_wait
          %input{:name => "first", :type => "hidden", :value => reservation.name}
          %input{:name => "body", :type => "text"}
          %button Send Text
/ %table
/   %tr
/     %th{:width => "150"} Name
/     %th{:width => "150"} Telephone Number
/     %th{:width => "150"} Estimated Wait
/     %th{:width => "30"} Wait
/     %th{:width => "75"} Cancel
/     %th{:width => "150"} Message
/     %th Send Text
/   - @reservations.each do |r|
/     %tr
/       %td.tablehead= r.name
/       %td.tablehead= r.phone
/       %td.tablehead= r.est_wait
/       %td.tablehead= r.est_wait
/       %td.tablehead= link_to('Cancel', '#', :'data-clear-form' => true, :class => 'button tiny radius alert')
/       %td.tablehead
/         = form_tag(sendtext_reservations_path)
/         = hidden_field_tag :phone, r.phone
/       %td.tablehead
/         = submit_tag 'Text', :class => 'button radius tiny'

