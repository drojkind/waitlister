= form_tag '/login', :remote => true do
  = label_tag :email
  = text_field_tag :email, nil, :autofocus => true
  = label_tag :password
  = password_field_tag :password
  = link_to('X', '#', :class => 'close-reveal-modal')
  = submit_tag 'Start The Queue', :class => 'button tiny radius success'
